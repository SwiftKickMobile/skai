---
name: skai-ui-map-implementation
description: Aligns app code to an approved UI Map using skai (audit against the proposed map, placeholder scaffolds, checklisted code changes and handoffs, build verification, and change requests back to architecture). Use when the user asks to implement, scaffold, or audit code against an approved UI Map.
---

# SKAI: UI Map implementation

## Instructions

- Read `Submodules/skai/Guides/UIMap/ui-map-implementation.md` and follow it.
- Preserve its hard boundaries before acting: structure and navigation only, no feature behavior; keep unrelated work out of the artifact; finish the UI Map workflow through `🏁` when it can proceed, or return its blocked gate before session orchestration resumes unrelated work; write code only after the Code Changes gate advances; create a map change request only after the operator explicitly approves that Discussion decision.
- Use SKAI-native defaults from the UI Map guides when no project-specific override exists.
- Read `skai/integration.md` for project-specific build/test commands, overrides, paths, and evidence requirements. STOP to ask only for missing values that are genuinely project-specific.
